# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — never commit secrets or a live Supabase project's keys
.env
.env*.local
.env.development.local
.env.production.local
# ...but DO track the no-secrets template (overrides the root .gitignore's .env* rule)
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts
